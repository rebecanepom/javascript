const names = ['JK', 'V', 'RM', 10, false]

const v = names[1]

names.push('JM') // add no final da lista

names.unshift(20) // add no inicio da lista

names.pop() // remove ultimo item da lista

names[3] = 'JIN' // mudar valor de tal item

console.log(names.indexOf('JK')) // procurar item

names.sort() // organizar lista em ordem

console.log(names)